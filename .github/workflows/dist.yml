name: Build Project
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install & Build
        run: |
          npm install
          npm run build
      - name: Upload Build
        uses: actions/upload-artifact@v3
        with:
          name: dist-files
          path: dist/
